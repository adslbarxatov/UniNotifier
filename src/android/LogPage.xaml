<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RD_AAOW.LogPage">

	<StackLayout>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="50" />
			</Grid.RowDefinitions>

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="50" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="50" />
			</Grid.ColumnDefinitions>

			<Button Grid.Row="0" Grid.Column="0" x:Name="GetGMJ" VerticalOptions="Center" />
			<Label Grid.Row="0" Grid.Column="1" x:Name="HeaderLabel" HorizontalOptions="Center" VerticalOptions="Center"/>
			<Button Grid.Row="0" Grid.Column="2" x:Name="AllNewsButton" VerticalOptions="Center" />
		</Grid>

		<!--<Grid x:Name="MainGrid">

			<Grid.RowDefinitions />

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="1" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<StackLayout Grid.Row="0" Grid.Column="0" Orientation="Vertical" HorizontalOptions="Start" VerticalOptions="Center">
				<Button x:Name="AllNewsButton" HorizontalOptions="Center" />
				<Label />
				<Button x:Name="GetGMJ" HorizontalOptions="Center"/>
			</StackLayout>-->

		<ListView x:Name="MainLog" HorizontalOptions="Fill" />

		<!--</Grid>-->
	</StackLayout>
</ContentPage>
    